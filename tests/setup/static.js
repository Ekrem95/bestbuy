const item = {
  name: 'testItem', price: '30', quantity: 25,
  tags: ['tag1', 'tag2'], src: 'img.source',
};

const user = {
      name: 'testuser', email: 'test@user.com', password: 'password',
    };

module.exports.user = user;
module.exports.item = item;
